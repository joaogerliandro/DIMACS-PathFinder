#pragma once

#include <entities.h>

using namespace Entities;

namespace Algorithm
{
    class BFS
    {
        public:
            static void bfs_algorithm(Node *first_node, uint64_t last_value)
            {
                
            }
    };
}